// import { Router } from 'express';
// import { enrollmentController } from '../controllers';
// import { auth } from '../middleware/auth';

// const router = Router();

// router.post('/courses/:courseId/enroll', auth, enrollmentController.create);
// router.get('/users/:userId/enrollments', auth, enrollmentController.getUserEnrollments);

// export default router;

import { Router } from 'express';
const router = Router();
export default router;