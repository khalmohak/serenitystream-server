// import { Router } from 'express';
// import { progressController } from '../controllers';
// import { auth } from '../middleware/auth';

// const router = Router();

// router.post('/content/:contentId/progress', auth, progressController.update);
// router.get('/users/:userId/progress', auth, progressController.getUserProgress);

// export default router;

import { Router } from 'express';
const router = Router();
export default router;